---
phase: 01-theme-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/theme_colors.dart
  - lib/theme/theme_definitions.dart
  - lib/providers/theme_provider.dart
  - lib/main.dart
  - lib/theme/app_theme.dart
autonomous: true
requirements:
  - THME-01
  - THME-02
  - THME-03
  - THME-04
  - THME-05
  - THME-06
  - THME-07
  - THME-08
user_setup:
  - service: shared_preferences
    why: "Theme persistence storage"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "用户启动应用时能看到已保存的主题颜色（无闪烁）"
    - "ThemeProvider 管理当前主题状态并在变更时通知监听器"
    - "4种主题（Cyber Blue、Red、Orange、Light）已定义所有颜色token"
    - "主题偏好在应用重启后持久化"
    - "main.dart 中的 ThemeData 使用 AppTheme 常量（无重复）"
  artifacts:
    - path: "lib/models/theme_colors.dart"
      provides: "ThemeColors 数据类，包含所有主题颜色 token"
      min_lines: 60
    - path: "lib/theme/theme_definitions.dart"
      provides: "4种主题的静态定义（Cyber Blue、Red、Orange、Light）"
      min_lines: 80
    - path: "lib/providers/theme_provider.dart"
      provides: "ThemeProvider 状态管理和持久化"
      min_lines: 50
    - path: "lib/main.dart"
      provides: "ThemeProvider 集成到 MultiProvider"
      contains: "ThemeProvider"
    - path: "lib/theme/app_theme.dart"
      provides: "支持动态颜色的 AppTheme 方法"
      contains: "surfaceBorder"
  key_links:
    - from: "lib/main.dart"
      to: "ThemeProvider"
      via: "MultiProvider"
      pattern: "ChangeNotifierProvider.*ThemeProvider"
    - from: "ThemeProvider"
      to: "theme_definitions.dart"
      via: "_getThemeColors"
      pattern: "ThemeColors\\.(cyberBlue|red|orange|light)"
    - from: "ThemeProvider"
      to: "SharedPreferences"
      via: "initialize/setTheme"
      pattern: "prefs\\.setString"
---

<objective>
创建主题系统基础设施：ThemeProvider、ThemeColors 模型、4种主题定义和持久化层。

Purpose: 为 Phase 2（组件迁移）和 Phase 3（设置页面）提供基础架构支持
Output: 完整的主题系统，包括状态管理、主题定义和持久化
</objective>

<execution_context>
@/Users/dangliuhui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dangliuhui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-theme-infrastructure/01-RESEARCH.md
@lib/theme/app_theme.dart
@lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ThemeColors 数据类和 4 种主题定义</name>
  <files>lib/models/theme_colors.dart, lib/theme/theme_definitions.dart</files>
  <action>
1. 创建 `lib/models/theme_colors.dart`:
   - 定义 `ThemeColors` 数据类，包含所有颜色 token（primary, backgroundDark, surface, accentCyan, accentRed, accentOrange, accentGreen, textPrimary, textSecondary, textMuted, accent）
   - 实现 copyWith 方法，遵循 CLAUDE.md 中的模型规范
   - 使用 const 构造函数

2. 创建 `lib/theme/theme_definitions.dart`:
   - 定义 4 种静态 ThemeColors 实例：
     - cyberBlue: primary=#0DA6F2, backgroundDark=#0A1114, accent=#0DA6F2
     - red: primary=#FF4D4D, backgroundDark=#0A1114, accent=#FF4D4D
     - orange: primary=#FFAB40, backgroundDark=#0A1114, accent=#FFAB40
     - light: primary=#0DA6F2, backgroundDark=#FFFFFF, accent=#0DA6F2
   - 参考 RESEARCH.md 中的颜色定义
   - 导出 AppThemeType 枚举

3. 修改现有 `lib/theme/app_theme.dart`:
   - 保留 surfaceBorder 和 glowShadow 方法，添加接受 ThemeColors 参数的重载版本
   - 保持向后兼容
  </action>
  <verify>
    <automated>flutter analyze lib/models/theme_colors.dart lib/theme/theme_definitions.dart</automated>
  </verify>
  <done>
    - ThemeColors 类包含所有必需的颜色字段和 copyWith 方法
    - 4 种主题已定义且颜色值正确
    - flutter analyze 无 error
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 ThemeProvider 并添加 shared_preferences 持久化</name>
  <files>lib/providers/theme_provider.dart</files>
  <action>
1. 创建 `lib/providers/theme_provider.dart`:
   - 定义 `AppThemeType` 枚举（cyberBlue, red, orange, light）
   - 继承 ChangeNotifier（遵循 LogProvider 模式）
   - 实现 initialize() 方法从 SharedPreferences 加载保存的主题
   - 实现 setTheme(AppThemeType) 方法保存并切换主题
   - 提供 currentColors getter 返回当前 ThemeColors
   - 提供 isDarkMode getter 判断是否为深色模式
   - 使用 `shared_preferences` 包进行持久化

2. 添加依赖：
   - 在 pubspec.yaml 中添加 `shared_preferences: ^2.2.0`（如果尚未添加）

3. 遵循 CLAUDE.md 中的 Provider 规范：
   - 使用 late final 或工厂函数初始化
   - 初始化前检查 _isInitialized 避免重复初始化
  </action>
  <verify>
    <automated>flutter analyze lib/providers/theme_provider.dart</automated>
  </verify>
  <done>
    - ThemeProvider 正确继承 ChangeNotifier
    - initialize() 从 SharedPreferences 加载主题
    - setTheme() 保存并通知监听器
    - flutter analyze 无 error
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成 ThemeProvider 到 main.dart</name>
  <files>lib/main.dart</files>
  <action>
1. 修改 `lib/main.dart`:
   - 导入 ThemeProvider
   - 在 MultiProvider 中添加 ChangeNotifierProvider<ThemeProvider>
   - 创建 OBDDashboardApp 使用 Consumer<ThemeProvider> 构建动态 ThemeData
   - 在 MaterialApp 中使用 themeProvider.currentColors 和 themeProvider.isDarkMode
   - 确保字体使用 GoogleFonts.spaceGrotesk()

2. 确保初始化顺序：
   - ThemeProvider 应该在其他依赖它的 Provider 之前初始化
   - 在 runApp 之前调用 ThemeProvider.initialize()（可选，或使用 lazy init）

3. 测试验证：
   - 应用启动时主题应该从持久化存储加载
   - 主题变更应该立即反映在 UI 上
  </action>
  <verify>
    <automated>flutter analyze lib/main.dart</automated>
  </verify>
  <done>
    - ThemeProvider 已添加到 MultiProvider
    - OBDDashboardApp 使用 Consumer 构建动态 ThemeData
    - ThemeData 正确使用 currentColors 和 isDarkMode
    - flutter analyze 无 error
  </done>
</task>

</tasks>

<verification>
- [ ] flutter analyze 在所有修改的文件上无 error
- [ ] 应用可以正常启动（flutter run 或构建测试）
- [ ] 主题切换功能在内存中正常工作
</verification>

<success_criteria>
- [ ] THME-01: ThemeColors 数据类已创建，包含所有颜色 token 和 copyWith 方法
- [ ] THME-02: 4 种主题已定义（Cyber Blue、Red、Orange、Light）
- [ ] THME-03: ThemeProvider 继承 ChangeNotifier，管理主题状态
- [ ] THME-04: 主题偏好使用 shared_preferences 持久化
- [ ] THME-05: Cyber Blue 主题颜色正确（深色背景 + 蓝色强调）
- [ ] THME-06: Red 主题颜色正确（深色背景 + 红色强调）
- [ ] THME-07: Orange 主题颜色正确（深色背景 + 橙色强调）
- [ ] THME-08: Light 主题颜色正确（白色背景 + 黑色文字）
</success_criteria>

<output>
After completion, create `.planning/phases/01-theme-infrastructure/01-theme-infrastructure-SUMMARY.md`
</output>
