---
phase: 03-settings-auto-switch
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [lib/screens/settings_screen.dart, lib/screens/main_container.dart, lib/main.dart]
autonomous: true
requirements: [THME-09, THME-10, THME-11, THME-21, THME-22]
user_setup: []
must_haves:
  truths:
    - "User can manually select theme from 4 options in settings"
    - "Theme selection takes effect immediately without app restart"
    - "Status bar brightness syncs with theme: light theme = dark status bar, dark theme = light status bar"
  artifacts:
    - path: "lib/screens/settings_screen.dart"
      provides: "Theme selector UI with 4 options and auto-switch toggle"
      contains: "RadioListTile", "SwitchListTile", "Consumer<ThemeProvider>"
    - path: "lib/main.dart"
      provides: "Status bar brightness synchronization"
      contains: "SystemChrome.setSystemUIOverlayStyle"
  key_links:
    - from: "lib/screens/settings_screen.dart"
      to: "lib/providers/theme_provider.dart"
      via: "Consumer<ThemeProvider>"
      pattern: "themeProvider\\.setTheme|themeProvider\\.setAutoSwitch"
    - from: "lib/main.dart"
      to: "lib/providers/theme_provider.dart"
      via: "addListener"
      pattern: "addListener.*_updateStatusBar"
---

<objective>
Create SettingsScreen UI and integrate status bar synchronization with theme.

Purpose: User can manually select theme and see status bar brightness sync with theme.
Output: SettingsScreen with theme selector, navigation integration, status bar sync
</objective>

<execution_context>
@/Users/dangliuhui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dangliuhui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/providers/theme_provider.dart
@lib/screens/main_container.dart
@lib/theme/app_theme.dart
@.planning/phases/03-settings-auto-switch/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsScreen with theme selector</name>
  <files>lib/screens/settings_screen.dart</files>
  <action>
    Create lib/screens/settings_screen.dart:
    1. Use Consumer<ThemeProvider> pattern
    2. Scaffold with backgroundColor: themeProvider.currentColors.backgroundDark
    3. AppBar with title "设置" and backgroundColor matching theme
    4. ListView with sections:
       a. Theme Selection Section:
          - Title: "主题选择"
          - 4 RadioListTile options for each AppThemeType
          - Show theme name and preview color swatch
          - On tap: call themeProvider.setTheme(selectedTheme)
       b. Auto Switch Section:
          - Title: "自动切换"
          - SwitchListTile for "根据日出日落自动切换"
          - Subtitle: "日出后使用浅色主题，日落后使用深色主题"
          - On changed: call themeProvider.setAutoSwitch(value)
    5. Use ListTileTheme and SwitchThemeData for consistent styling
    6. Use themeProvider.currentColors for all colors

    Reference: 03-RESEARCH.md Pattern 1: Settings Screen Pattern
  </action>
  <verify>
    <automated>flutter analyze lib/screens/settings_screen.dart</automated>
  </verify>
  <done>SettingsScreen with theme selector UI (THME-09, THME-10, THME-11)</done>
</task>

<task type="auto">
  <name>Task 2: Add SettingsScreen to navigation</name>
  <files>lib/screens/main_container.dart</files>
  <action>
    Modify lib/screens/main_container.dart:
    1. Import settings_screen.dart
    2. Add SettingsScreen to the IndexedStack pages list
    3. Add navigation item for Settings (use icons.settings or similar)
    4. Update _NavItem icons to include settings icon

    Reference existing navigation structure in main_container.dart.
  </action>
  <verify>
    <automated>flutter analyze lib/screens/main_container.dart</automated>
  </verify>
  <done>SettingsScreen accessible via navigation</done>
</task>

<task type="auto">
  <name>Task 3: Integrate status bar sync in main.dart</name>
  <files>lib/main.dart</files>
  <action>
    Modify lib/main.dart:
    1. Import 'package:flutter/services.dart' for SystemChrome
    2. Add _updateStatusBar method that:
       - Checks themeProvider.isDarkMode
       - Calls SystemChrome.setSystemUIOverlayStyle:
         - Dark theme: statusBarBrightness: Brightness.dark, statusBarIconBrightness: Brightness.light
         - Light theme: statusBarBrightness: Brightness.light, statusBarIconBrightness: Brightness.dark
    3. Call _updateStatusBar in themeProvider.initialize() completion
    4. Add listener to ThemeProvider to call _updateStatusBar on theme change:
       - themeProvider.addListener(_onThemeChanged)
       - _onThemeChanged calls _updateStatusBar

    Reference: 03-RESEARCH.md Pattern 3: Status Bar Sync
    IMPORTANT: Clean up listener in dispose() to prevent memory leaks.
  </action>
  <verify>
    <automated>flutter analyze lib/main.dart</automated>
  </verify>
  <done>Status bar brightness syncs with theme (THME-21, THME-22)</done>
</task>

</tasks>

<verification>
All files pass `flutter analyze` with no errors.
</verification>

<success_criteria>
1. SettingsScreen shows 4 theme options with radio buttons
2. Selecting theme calls themeProvider.setTheme() - immediate effect (no restart)
3. Auto-switch toggle calls themeProvider.setAutoSwitch()
4. Settings accessible via bottom navigation
5. Status bar brightness synced: light theme = dark status bar, dark theme = light status bar
</success_criteria>

<output>
After completion, create `.planning/phases/03-settings-auto-switch/{phase}-02-SUMMARY.md`
</output>
