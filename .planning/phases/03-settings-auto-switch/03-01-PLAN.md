---
phase: 03-settings-auto-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pubspec.yaml, android/app/src/main/AndroidManifest.xml, lib/services/sunrise_sunset_service.dart, lib/providers/theme_provider.dart]
autonomous: true
requirements: [THME-12, THME-13, THME-14]
user_setup: []
must_haves:
  truths:
    - "User can enable auto-switch to automatically change theme based on sunrise/sunset"
    - "App calculates sunrise/sunset times using device location"
    - "After sunrise theme switches to light, after sunset theme switches to dark"
  artifacts:
    - path: "lib/services/sunrise_sunset_service.dart"
      provides: "Location-based sunrise/sunset time calculation"
      exports: ["SunriseSunsetService", "getSunriseSunsetTimes()"]
    - path: "lib/providers/theme_provider.dart"
      provides: "Auto-switch state management with persistence"
      contains: "isAutoSwitchEnabled", "setAutoSwitch", "checkAutoSwitch"
  key_links:
    - from: "lib/providers/theme_provider.dart"
      to: "lib/services/sunrise_sunset_service.dart"
      via: "SunriseSunsetService injection"
      pattern: "setSunriseSunsetService"
---

<objective>
Add dependencies and create core infrastructure for sunrise/sunset-based auto theme switching.

Purpose: Enable automatic theme switching based on sunrise/sunset times at user's location.
Output: SunriseSunsetService, extended ThemeProvider with auto-switch methods
</objective>

<execution_context>
@/Users/dangliuhui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dangliuhui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-theme-infrastructure/01-01-SUMMARY.md
@.planning/phases/03-settings-auto-switch/03-RESEARCH.md
@lib/providers/theme_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and Android permissions</name>
  <files>pubspec.yaml, android/app/src/main/AndroidManifest.xml</files>
  <action>
    1. Add dependencies to pubspec.yaml:
       - geolocator: ^12.0.0
       - sunrise_sunset_calc: ^1.0.0
    2. Add Android permissions to AndroidManifest.xml:
       - ACCESS_FINE_LOCATION
       - ACCESS_COARSE_LOCATION
    3. Run `flutter pub get` to install dependencies

    NOTE: Do NOT add BLUETOOTH permissions - these are already present for OBD.
  </action>
  <verify>
    <automated>flutter pub get && grep -q "geolocator" pubspec.yaml</automated>
  </verify>
  <done>Dependencies installed and Android permissions added</done>
</task>

<task type="auto">
  <name>Task 2: Create SunriseSunsetService</name>
  <files>lib/services/sunrise_sunset_service.dart</files>
  <action>
    Create lib/services/sunrise_sunset_service.dart with:
    - Class SunriseSunsetService
    - Method getSunriseSunsetTimes() that:
      - Uses geolocator to get current position (low accuracy, 10s timeout)
      - Uses sunrise_sunset_calc to calculate sunrise/sunset for today
      - Returns SunriseSunset object with sunrise and sunset DateTime
    - Handles LocationServiceDisabledException and PermissionDenied exceptions
    - Returns null on failure (graceful degradation)

    Reference the service structure from 03-RESEARCH.md Pattern 2.
  </action>
  <verify>
    <automated>flutter analyze lib/services/sunrise_sunset_service.dart</automated>
  </verify>
  <done>SunriseSunsetService created with getSunriseSunsetTimes() method</done>
</task>

<task type="auto">
  <name>Task 3: Extend ThemeProvider with auto-switch functionality</name>
  <files>lib/providers/theme_provider.dart</files>
  <action>
    Modify lib/providers/theme_provider.dart to add:
    1. Private fields:
       - SunriseSunsetService _sunriseSunsetService
       - bool _isAutoSwitchEnabled
       - Timer? _autoSwitchTimer

    2. Getter:
       - bool get isAutoSwitchEnabled => _isAutoSwitchEnabled

    3. Methods:
       - void setSunriseSunsetService(SunriseSunsetService service) - inject service
       - Future<void> setAutoSwitch(bool enabled) - enable/disable auto-switch
       - Future<void> checkAutoSwitch() - check time and apply theme
       - Future<void> startAutoSwitchTimer() - start periodic check (every 5 min)

    4. Auto-switch logic:
       - If enabled, check if current time is after sunrise -> light theme
       - If enabled, check if current time is after sunset -> dark theme (cyberBlue)
       - Only change theme if different from current

    5. Persistence:
       - Save _isAutoSwitchEnabled to SharedPreferences with key 'auto_theme_switch'
       - Load on initialize()

    NOTE: Use AppThemeType.light for daytime, AppThemeType.cyberBlue for nighttime.
    IMPORTANT: Import sunrise_sunset_calc package.
  </action>
  <verify>
    <automated>flutter analyze lib/providers/theme_provider.dart</automated>
  </verify>
  <done>ThemeProvider extended with auto-switch methods and persistence</done>
</task>

</tasks>

<verification>
All files pass `flutter analyze` with no errors.
</verification>

<success_criteria>
1. geolocator and sunrise_sunset_calc dependencies added
2. Android location permissions added
3. SunriseSunsetService created with getSunriseSunsetTimes()
4. ThemeProvider has isAutoSwitchEnabled getter and setAutoSwitch() method
5. Auto-switch logic: sunrise -> light theme, sunset -> dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/03-settings-auto-switch/{phase}-01-SUMMARY.md`
</output>
