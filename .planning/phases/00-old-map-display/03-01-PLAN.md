---
phase: 03-map-display
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/widgets/trajectory/map_widget.dart
  - lib/widgets/trajectory/current_location_marker.dart
  - lib/screens/trajectory_detail_screen.dart
  - lib/theme/app_theme.dart
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04

must_haves:
  truths:
    - User can view recorded track rendered as neon cyan polyline on map
    - User can see current location with direction arrow during recording
    - Map displays with dark theme (CartoDB Dark Matter tiles)
    - User can zoom and pan the map freely
  artifacts:
    - path: "pubspec.yaml"
      provides: "flutter_map dependency added"
    - path: "lib/widgets/trajectory/map_widget.dart"
      provides: "Main map widget with dark theme and track rendering"
      exports: ["TrajectoryMapWidget"]
    - path: "lib/widgets/trajectory/current_location_marker.dart"
      provides: "Direction arrow marker for current location"
      exports: ["CurrentLocationMarker"]
    - path: "lib/screens/trajectory_detail_screen.dart"
      provides: "Full screen map view with recorded trajectory"
  key_links:
    - from: "lib/widgets/trajectory/map_widget.dart"
      to: "lib/models/trajectory.dart"
      via: "trajectory.points list"
      pattern: "trajectory\\.points"
    - from: "lib/widgets/trajectory/map_widget.dart"
      to: "lib/services/location_service.dart"
      via: "GPS location stream"
      pattern: "LocationService"
    - from: "lib/screens/trajectory_detail_screen.dart"
      to: "lib/widgets/trajectory/map_widget.dart"
      via: "TrajectoryMapWidget usage"
      pattern: "TrajectoryMapWidget"
---

<objective>
Display recorded ride trajectory on a map with cyberpunk theme. Implement track polyline rendering, current location marker with direction, dark-themed map tiles, and zoom/pan interactions.
</objective>

<context>
@.planning/phases/03-map-display/03-RESEARCH.md
@.planning/phases/03-map-display/03-CONTEXT.md
@/Users/dangliuhui/Desktop/tttzzz/moto_code/dashboard_test01/lib/models/trajectory.dart
@/Users/dangliuhui/Desktop/tttzzz/moto_code/dashboard_test01/lib/theme/app_theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add flutter_map dependency</name>
  <files>pubspec.yaml</files>
  <action>
    Add flutter_map: ^7.0.0 to pubspec.yaml dependencies section.
    Run flutter pub get to install the package.
    Note: latlong2 is already present in pubspec.yaml (^0.9.1).
  </action>
  <verify>
    <automated>flutter pub get && grep -q "flutter_map" pubspec.yaml</automated>
  </verify>
  <done>flutter_map added to pubspec.yaml</done>
</task>

<task type="auto">
  <name>Task 2: Create CurrentLocationMarker widget</name>
  <files>lib/widgets/trajectory/current_location_marker.dart</files>
  <action>
    Create a widget that displays current GPS location with direction arrow.
    Use Transform.rotate with angle = -heading * pi / 180 (negative for correct direction).
    Arrow should be neon cyan (#00F2FF) with black icon.
    Use Container with BoxShape.circle and Icons.arrow_upward.
    Accept heading parameter in degrees (0 = north, clockwise).
  </action>
  <verify>
    <automated>ls lib/widgets/trajectory/current_location_marker.dart</automated>
  </verify>
  <done>CurrentLocationMarker widget created with rotation support</done>
</task>

<task type="auto">
  <name>Task 3: Create TrajectoryMapWidget</name>
  <files>lib/widgets/trajectory/map_widget.dart</files>
  <action>
    Create main map widget using flutter_map.

    Key implementations:
    1. Use FlutterMap with MapController for zoom/pan (MAP-04)
    2. TileLayer: CartoDB Dark Matter URL 'https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png' (MAP-03)
    3. PolylineLayer: Track as neon cyan (#00F2FF) polyline with 4px strokeWidth (MAP-01)
    4. MarkerLayer: Current location with direction arrow using CurrentLocationMarker (MAP-02)
    5. Implement auto-fit track bounds on initial load using CameraFit.bounds()
    6. Implement follow mode: when followMode=true, map centers on current location

    Parameters:
    - trajectoryPoints: List of GPSPoint (from Trajectory model)
    - currentLocation: LatLng? for live location during recording
    - currentHeading: double? for direction arrow rotation
    - followMode: bool to enable auto-follow during recording

    Use latlong2 package for LatLng conversion.
  </action>
  <verify>
    <automated>ls lib/widgets/trajectory/map_widget.dart</automated>
  </verify>
  <done>TrajectoryMapWidget with track polyline, dark tiles, and location marker</done>
</task>

<task type="auto">
  <name>Task 4: Integrate map into TrajectoryDetailScreen</name>
  <files>lib/screens/trajectory_detail_screen.dart</files>
  <action>
    Update TrajectoryDetailScreen to use TrajectoryMapWidget for displaying the recorded trajectory.
    Pass trajectory points from the selected trajectory.
    Set followMode=false for viewing historical rides (fixed view).
    Use fitTrackBounds to show entire track on initial load.

    If trajectory is empty, show placeholder message.
  </action>
  <verify>
  <automated>flutter analyze lib/screens/trajectory_detail_screen.dart</automated>
  </verify>
  <done>TrajectoryDetailScreen displays map with recorded track</done>
</task>

</tasks>

<verification>
- [ ] Track displays as neon cyan polyline on dark map
- [ ] Current location shows direction arrow during recording
- [ ] Map uses CartoDB Dark Matter tiles (dark theme)
- [ ] Zoom and pan work via MapController
- [ ] Track fits in view on initial load
</verification>

<success_criteria>
- flutter_map dependency installed
- TrajectoryMapWidget renders track polyline with neon cyan color
- Dark themed map tiles load correctly
- Current location marker shows direction
- User can zoom/pan map freely
</success_criteria>

<output>
After completion, create `.planning/phases/03-map-display/03-01-SUMMARY.md`
</output>
